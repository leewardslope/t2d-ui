---
- hosts: all
  tasks:
    - name: Adding the buildpack => {{ buildpackName }}
      shell: dokku buildpacks:add {{ appTitle }} {{ buildpackUrl }}
      ignore_errors: True
      async: 600 # Maximum allowed time in Seconds
      poll: 15